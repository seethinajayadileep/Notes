-- Write your PostgreSQL query statement below
select u.name,Coalesce(sum(distance),0) as travelled_distance from rides r right join users u on r.user_id=u.id group by u.name order by  travelled_distance desc ,u.name ;
