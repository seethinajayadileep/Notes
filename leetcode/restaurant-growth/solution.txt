-- Write your PostgreSQL query statement below
select distinct visited_on,(sum(amount) over(order by visited_on  range between interval '6' DAY preceding and current row)) as amount, round((sum(amount) over(order by visited_on  range between interval '6' DAY preceding and current row))/7::numeric,2)  as average_amount  from customer   order by visited_on offset 6;