-- Write your PostgreSQL query statement below
select p.product_name ,o.sum_unit as unit from products p join
(select product_id,sum(unit) as sum_unit from orders where EXTRACT(MONTH from order_date)=2  group by product_id having sum(unit)>=100) as o on p.product_id=o.product_id;
